Note, this software makes use of git submodules.

Please clone this by using:
git clone --recursive https://github.com/zachbornheimer/ANUBIS.git;

If that doesn't work, once you have cloned the repo, run:
git submodule init; git submodule update;

##############
### ANUBIS ###
##############

This is the dev branch for ANUBIS.

The dev branch is designed to be the place where the unstable things get stable.

Note, the receptor.anubis file is actually a perl5 script.

Current Features in progress:
mod.xml and config.xml integration (server storage and modification)
Making sure that the user is protected from unauthorized actions
